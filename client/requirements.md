## Packages
framer-motion | Complex animations for futuristic UI feel
clsx | Utility for conditional class names (already in base but good to be explicit)
tailwind-merge | Utility for merging tailwind classes (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Oxanium'", "sans-serif"],
  body: ["'Space Grotesk'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
}
